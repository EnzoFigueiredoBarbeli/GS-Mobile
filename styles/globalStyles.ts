import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20
  },
  title: {
    fontSize: 28,
    fontWeight: "bold",
    marginBottom: 20
  },
  text: {
    fontSize: 16,
    marginBottom: 20
  },
  input: {
    borderWidth: 1,
    padding: 12,
    marginBottom: 15,
    borderRadius: 8
  },
  card: {
    backgroundColor: "#EEE",
    padding: 20,
    borderRadius: 12,
    marginBottom: 15
  },
  cardTitle: {
    fontSize: 18,
    fontWeight: "bold",
    marginBottom: 8
  }
});
